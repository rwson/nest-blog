import styled from '@emotion/styled';

export const CommentTreeContainer = styled.div({
  '.comment-place': {
    margin: '15px 0 0'
  }
});

export const CommentTextAfterIcon = styled.span({
  padding: '0 0 0 5px'
});
